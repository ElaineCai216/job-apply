# job-apply 仓库规则

- **改完必推**：任何代码 / 样式 / 脚本 / README / docs 改动，验证通过后必须 `git push origin main`，GitHub Pages 会自动重新构建。
- 推送后确认 Pages 构建为 `built` 且线上可访问（HTTP 200）。
- 文档在 `docs/` 目录（简历针对性调整 SOP、求职填报系统说明），改动需同步提交。
- 本仓库为**公开**仓库：禁止提交任何个人数据（简历文件、投递记录、联系方式、内推码等）。

## 执行边界

- 只处理求职工作台、岗位搜集、材料定制、面试准备和相关文档。
- 最终投递、发送邮件、真实性声明、敏感问卷必须逐份获得用户确认。
- 不绕过验证码、登录保护或招聘网站安全机制。
- 不捏造经历、数字、技能、日期、联系方式或内推信息；不确定内容标记为待确认。
- 个人资料、岗位记录、内推码、邮箱、面经和密钥不得进入 Git、构建产物或日志。
- 每次改动必须测试、更新 README，并验证本地或线上入口。
- 涉及外部发送、账户授权、付费或删除数据时必须先询问。
