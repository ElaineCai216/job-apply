# BUG-006：Safari 多平台配对码无法被扩展识别

- 级别：P1
- 现象：App 生成多平台结构的配对码后，Safari 扩展仍按旧版单平台格式校验，导致无法保存并扫描。
- 根因：扩展弹窗只识别 `endpoint` 和 `token` 顶层字段；3.2 的配对码改为 `pairings.{portal}`。
- 修复：扩展兼容旧 JobsDB 配对码和新版多平台配对码，并根据当前页面域名选择相应令牌；服务端返回安全的数据库错误代码，避免前端吞掉原因。
- 验证：构建、扩展脚本语法检查、部署后重新生成配对码。Safari 扩展仍需由用户在 Xcode 安装新版后实际确认。
